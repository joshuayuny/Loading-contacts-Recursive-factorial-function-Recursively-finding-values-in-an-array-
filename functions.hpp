#include <string>
using namespace std;
void loadContacts(string filename);

int factorial(int num);

bool nameExists(string names[], int size, string searchName);

int findName(string name[], int size, string searchName);

int findContactFile(string filename, string searchName);
